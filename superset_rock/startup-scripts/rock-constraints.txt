# Security overrides for pins in Superset's requirements/base.txt.
# Each line replaces the matching pin in base.txt and is also applied as a pip
# constraint when the superset part resolves its own dependencies, so both
# Python trees in the rock get the same versions. Keep versions within the
# ranges declared by Superset's pyproject.toml.
apsw==3.53.4.0
cryptography==46.0.7
mako==1.3.12
markdown==3.8.2
pillow==12.3.0
pyasn1==0.6.4
pyjwt==2.13.0
pyopenssl==26.0.0
setuptools==84.0.0
urllib3==2.7.0
