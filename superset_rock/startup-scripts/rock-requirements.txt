# Required dependencies missing from Superset requirements.txt
jmespath==1.0.1

# Optional dependencies for Database connectors
# Built from source against Ubuntu's libpq: psycopg2-binary bundles its own
# OpenSSL, PCRE and Cyrus SASL builds from CentOS 7.
psycopg2==2.9.13
Authlib==1.6.12
elasticsearch-dbapi>=0.2.7,<0.3.0
trino==0.327.0
mysqlclient==2.1.1
pyhive==0.7.0
thrift==0.24.0
thrift-sasl>=0.4.3
sqlalchemy-redshift==0.8.1
gspread==6.1.2

# Monitoring
sentry-sdk==0.10.2
statsd==4.0.1

# Requirement for gevent worker class
gevent==24.2.1

# Alerts and reports screenshots (Playwright + Chromium)
playwright==1.63.0
